name: Build APK

# 云端 CI：推到 main 后自动编译出 APK，产物在 Actions 页面 Artifacts 下载
# 也可手动触发：Actions -> Build APK -> Run workflow

on:
  push:
  pull_request:
  workflow_dispatch:   # 允许手动触发

defaults:
  run:
    working-directory: udp_monitor_app   # Android 工程在该子目录下

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build debug APK
        run: ./gradlew assembleDebug --stacktrace

      - name: Build release APK
        run: ./gradlew assembleRelease --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: udp-monitor-debug-apk
          path: udp_monitor_app/app/build/outputs/apk/debug/*.apk

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: udp-monitor-release-apk
          path: udp_monitor_app/app/build/outputs/apk/release/*.apk